apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql_john
  labels:
    role: app
spec:
  replicas: 2
  selector:
    matchLabels:
      role: app
  template:
    metadata:
      labels:
        role: app
    spec:
      containers:
      - name: app
        image: johnpluznyk/mysql_john:latest
        resources:
          requests:
            cpu: 10m
